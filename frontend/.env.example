# Leave unset to route API calls through Vite's proxy (works for localhost and network IP).
# Only set this if you need to hit the backend directly without the proxy.
# VITE_API_BASE_URL=http://localhost:8080/api/v1
